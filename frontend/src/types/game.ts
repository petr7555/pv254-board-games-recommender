type Game = {
  id: number;
  name: string;
  yearPublished: number;
  difficulty: number;
  avgRating: number;
  minPlayers: number;
  maxPlayers: number;
  playtime: number;
  minAge: number;
  image: string;
};

export default Game;
  
  
